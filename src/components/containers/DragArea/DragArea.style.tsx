import styled from 'styled-components';

export const DragAreaStyled = styled.ul`
  margin: 0;
  padding: 25px;
  border-radius: 10px;
  max-width: 400px;
  background-color: rgba(255, 255, 255, 0.3);
  backdrop-filter: blur(10px);
`;
